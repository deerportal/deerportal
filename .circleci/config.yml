version: 2
jobs:
   build:
     docker:
       - image: ubuntu:xenial
     steps:
       - checkout
       - run: lsb_release -a
       - run: apt-get install cmake libsfml-dev -y
       - run: cmake .
